@{
    ViewData["Title"] = "Archery shop";
}
<main>
    @model List<string>

    <h1>Bow Types</h1>

    <ul>
        @foreach (var bowType in Model)
        {
            <li>@bowType</li>
        }
    </ul>
</main>